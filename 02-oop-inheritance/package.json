{
  "name": "training-ecs6",
  "version": "1.0.0",
  "description": "training ecmascript6",
  "scripts": {
    "build": "babel scripts -d build",
    "postbuild": "browserify build/main.js -o build/bundle.js"
  },
  "dependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "browserify": "^13.0.1"
  },
  "babel": {
    "presets": ["es2015"]
  }
}
